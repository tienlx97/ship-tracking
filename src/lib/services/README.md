# Services

Shared application services live here.

Services should contain reusable infrastructure or domain operations that are
not owned by a single route component. Keep feature-specific service wrappers in
the owning feature until another feature needs them.

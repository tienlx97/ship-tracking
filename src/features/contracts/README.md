# Contracts Feature

Owns the CIF sale contract workflow described in `docs/PRODUCT.md`.

Initial scope:

- Compose the contract workspace UI for the app route.
- Keep contract-specific components, schemas, hooks, API functions, and types in
  this feature folder.
- Export public feature entry points through `index.ts`.

Future implementation must keep fixed legal template text separate from
user-entered contract data.

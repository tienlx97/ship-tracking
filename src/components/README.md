# Shared Components

Reusable UI and layout components live here.

Rules:

- Use HeroUI for base components.
- Keep feature-specific UI inside `src/features/<feature>/components`.
- Move duplicated UI patterns here only after at least two callers need them.
